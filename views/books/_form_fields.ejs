<label>Name</label>
<input type="text" name="title" value="<%- book.title%>">
<label>Author</label>
<% authors.forEach(author => {%>
    <% if (author.id === book.autho){ %>
    <option label ="<% author.name %>" value="<%= author.id %>" >
    <% } else { %> 
    <option >
        <%})%> })%>
<input type="text" name="author" value="<%= book.author%>">